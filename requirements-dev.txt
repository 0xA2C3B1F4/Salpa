# Host tests, fixture-only signing tests, and optional USB acceptance tools.
# No operational keys or devices are required to install or test these packages.
cryptography==48.0.1
esptool==5.4.0
fido2==2.2.1
